/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
/public/ckeditor_wiris
/public/plugins
# Live-database artefacts — never committed.
# Real production data and host names. Kept locally for rollback only.
_local-backups/
docs/phase3/_changes/LIVE-*.sql
docs/phase3/_changes/*backup*.sql

# Any .env backup, however it is named - one of these blocked a push
# with a full credential set in it (DB, mail, Spaces, Gemini, DeepSeek).
.env.bak*
.env.*.bak
.env.local
.env.*.local

# Audit evidence may contain live tokens - never commit it.
Docs/hrit-audit/_evidence/tokens.tsv
Docs/hrit-audit/_evidence/unicode-payload.json
